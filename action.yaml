name: Neo Run Tests
description: Run tests and PEP8 check

runs:
  using: "composite"
  steps:
    - run: python -m pip install --upgrade pip
    - run: pip install -r requirements.txt
    - run: python -m unittest discover
    - run: |
        echo "Проверка кода на PEP8"
        flake8 app.py
